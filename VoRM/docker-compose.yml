services:
  app:
    build:
      context: ./
      dockerfile: dev.Dockerfile
    image: vorm_dev
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 3001:3001
      - 5173:5173
    container_name: vorm_dev
